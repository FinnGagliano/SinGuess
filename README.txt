Hello!

This is a neural network designed to return sin(x) if given x.

I will not ever tell it what the sin(x) function is, but by training it and adjusting it's weights
it will eventually become more and more accurate.
